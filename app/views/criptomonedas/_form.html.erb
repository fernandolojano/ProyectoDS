<%= form_with(model: criptomoneda) do |form| %>
  <% if criptomoneda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(criptomoneda.errors.count, "error") %> prohibited this criptomoneda from being saved:</h2>

      <ul>
        <% criptomoneda.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :token_criptomoneda %>
    <%= form.text_field :token_criptomoneda %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :valor_actual %>
    <%= form.text_field :valor_actual %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
