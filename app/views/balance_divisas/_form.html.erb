<%= form_with(model: balance_divisa) do |form| %>
  <% if balance_divisa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(balance_divisa.errors.count, "error") %> prohibited this balance_divisa from being saved:</h2>

      <ul>
        <% balance_divisa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>

    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id %>

    <%= form.label :token_divisa %>
    <%= form.text_field :token_divisa %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
